# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mutmut-cache/
mutants/

# Local scratch model downloads (the vendored PP-DocLayout-S asset lives
# under src/ocr_server/models/ and is tracked)
/models/

# Local scratch
output/
*.log
.DS_Store

# Web/desktop build outputs (the Tauri app lives in Paperhub now; nothing
# under ui/ is tracked anymore, but keep these guards so a stray
# `git add -A` can never sweep regenerable artifacts into history again)
node_modules/
dist/
target/
.tanstack/
coverage/
*.tsbuildinfo
playwright-report/
test-results/
